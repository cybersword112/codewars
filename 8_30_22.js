function sortByLength (array) {
    // Return an array containing the same strings, ordered from shortest to longest
    return array.sort((a,b)=>{return a.length - b.length})
  // sort the array, to sort compare length of a and b
  //   return the array
  };
  
  
  // will be provided an array of strings
  // output should be the same array with strings sorted by length