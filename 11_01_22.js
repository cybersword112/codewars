function rowSumOddNumbers(n) {
    //   this is purely a test of math knowledge, it would be alittle crazy to solve by most manual methods
      return Math.pow(n, 3);
    }
    
    // for a pyramid of consecutive odd numbers (1 being the point, 3, 5 being base level two etc)
    //  return the sum of the nth level of this pyramid